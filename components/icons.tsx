import { LucideIcon, LucideProps } from "lucide-react";
import React from "react";
import { motion } from "framer-motion";

export type Icon = LucideIcon | JSX.Element;

export const Icons = {
  grid: (props?: LucideProps, rectVariants: any) => (
    
  ),
};
